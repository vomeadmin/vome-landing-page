---
/**
 * Module landing page — App for volunteers.
 *
 * Mirrors the canonical /volunteer-communications composition. Copy lives
 * in `strings.module_app`. Hand-built mockups model real screens from
 * `mobile-app/VomeApp/src/screens/`:
 *   Hero        AppHomeMock         (volunteer home / dashboard)
 *   Spotlight 1 AppReserveMock      (shift detail + group reservation)
 *   Spotlight 2 AppQrArriveMock     (QR check-in on arrival)
 *   Spotlight 3 AppScheduleMock     (schedule + cancel + add time off)
 */
import Layout from "../../layouts/Layout.astro";
import Partners from "../sections/Partners.astro";
import Faq from "../sections/Faq.astro";
import FeatureMatrix from "../sections/FeatureMatrix.astro";
import ClosingCta from "../sections/ClosingCta.astro";
import ModuleHero from "../module/ModuleHero.astro";
import BenefitGrid from "../module/BenefitGrid.astro";
import FeatureSpotlight from "../module/FeatureSpotlight.astro";
import InlineCta from "../module/InlineCta.astro";
import Differentiators from "../module/Differentiators.astro";
import AppHomeMock from "../module/AppHomeMock.astro";
import AppReserveMock from "../module/AppReserveMock.astro";
import AppQrArriveMock from "../module/AppQrArriveMock.astro";
import AppScheduleMock from "../module/AppScheduleMock.astro";
import { strings, t, localeFromPath, type Locale } from "../../i18n";
import { appUrl } from "../../lib/appUrl";

interface Props {
  locale?: Locale;
}

const { locale = localeFromPath(Astro.url.pathname) } = Astro.props;
const r = strings.module_app;
const prefix = locale === "fr" ? "/fr" : "";
const demoHref = `${prefix}/demo`;
const signupHref = appUrl("/register");

// ── Hero meta pills (3) ──
const heroMeta = [
  { text: t(r.hero_meta_native, locale) },
  { text: t(r.hero_meta_free, locale) },
  { text: t(r.hero_meta_multiorg, locale) },
];

// ── Benefits grid (6 cards). Icons match the platform-diagram line style. ──
const benefits = [
  {
    // Magnifying glass over list (browse opportunities)
    iconSvg: '<circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/><path d="M7 11h8M7 8h8M7 14h5"/>',
    title: t(r.ben1_title, locale),
    desc: t(r.ben1_desc, locale),
  },
  {
    // Calendar + check (reserve)
    iconSvg: '<rect x="3" y="4" width="18" height="18" rx="2"/><path d="M3 10h18M8 2v4M16 2v4"/><path d="M8 14l3 3 5-5"/>',
    title: t(r.ben2_title, locale),
    desc: t(r.ben2_desc, locale),
  },
  {
    // QR grid (check-in)
    iconSvg: '<rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><path d="M14 14h3v3h-3zM20 14h1M14 20h3M20 17v4"/>',
    title: t(r.ben3_title, locale),
    desc: t(r.ben3_desc, locale),
  },
  {
    // Calendar with X (time off / cancel)
    iconSvg: '<rect x="3" y="4" width="18" height="18" rx="2"/><path d="M3 10h18M8 2v4M16 2v4"/><path d="M9 14l6 4M15 14l-6 4"/>',
    title: t(r.ben4_title, locale),
    desc: t(r.ben4_desc, locale),
  },
  {
    // Document + signature (onboarding)
    iconSvg: '<path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><path d="M14 2v6h6"/><path d="M8 16c1-3 3-3 5 0s4 2 4-1"/>',
    title: t(r.ben5_title, locale),
    desc: t(r.ben5_desc, locale),
  },
  {
    // Speech bubble + bell (chat + push)
    iconSvg: '<path d="M21 12a8 8 0 0 1-11.5 7.2L4 21l1.8-5.5A8 8 0 1 1 21 12z"/><path d="M14 8a2 2 0 014 0v3l1 1h-6l1-1V8z"/>',
    title: t(r.ben6_title, locale),
    desc: t(r.ben6_desc, locale),
  },
];

// ── Spotlight bullets ──
const spot1Bullets = [t(r.spot1_b1, locale), t(r.spot1_b2, locale), t(r.spot1_b3, locale), t(r.spot1_b4, locale)];
const spot2Bullets = [t(r.spot2_b1, locale), t(r.spot2_b2, locale), t(r.spot2_b3, locale), t(r.spot2_b4, locale)];
const spot3Bullets = [t(r.spot3_b1, locale), t(r.spot3_b2, locale), t(r.spot3_b3, locale), t(r.spot3_b4, locale)];

// ── Differentiators (3) ──
const differentiators = [
  {
    // Price tag (free)
    iconSvg: '<path d="M20.6 12.6L12 21.2l-9-9V3h9.2z"/><circle cx="7.5" cy="7.5" r="1.6"/>',
    title: t(r.diff1_title, locale),
    desc: t(r.diff1_desc, locale),
  },
  {
    // Stack of buildings (multi-org)
    iconSvg: '<path d="M3 21V8l6-3 6 3v13"/><path d="M15 21V11l6 3v7"/><path d="M3 21h18"/>',
    title: t(r.diff2_title, locale),
    desc: t(r.diff2_desc, locale),
  },
  {
    // Key (sign in)
    iconSvg: '<circle cx="8" cy="15" r="4"/><path d="M11 12l8-8M16 7l3 3M14 9l3 3"/>',
    title: t(r.diff3_title, locale),
    desc: t(r.diff3_desc, locale),
  },
];

// ── Module FAQs (6) ──
const faqItems = [
  { q: r.faq1_q, a: r.faq1_a, open: true },
  { q: r.faq2_q, a: r.faq2_a },
  { q: r.faq3_q, a: r.faq3_a },
  { q: r.faq4_q, a: r.faq4_a },
  { q: r.faq5_q, a: r.faq5_a },
  { q: r.faq6_q, a: r.faq6_a },
];
---

<Layout
  locale={locale}
  title={t(r.page_title, locale)}
  description={t(r.page_description, locale)}
>
  <ModuleHero
    locale={locale}
    eyebrow={t(r.hero_eyebrow, locale)}
    h1Part1={t(r.hero_h1_part1, locale)}
    h1Accent={t(r.hero_h1_accent, locale)}
    h1Part2={t(r.hero_h1_part2, locale)}
    sub={t(r.hero_sub, locale)}
    ctaPrimaryHref={signupHref}
    ctaPrimaryLabel={t(strings.nav.signup_free, locale)}
    ctaSecondaryHref={demoHref}
    ctaSecondaryLabel={t(strings.nav.book_demo, locale)}
    meta={heroMeta}
  >
    <AppHomeMock slot="mockup" locale={locale} />
  </ModuleHero>

  <Partners locale={locale} />

  <BenefitGrid
    locale={locale}
    h2={t(r.benefits_h2, locale)}
    sub={t(r.benefits_sub, locale)}
    items={benefits}
  />

  <FeatureSpotlight
    locale={locale}
    kicker={t(r.spot1_kicker, locale)}
    title={t(r.spot1_title, locale)}
    body={t(r.spot1_body, locale)}
    bullets={spot1Bullets}
    side="right"
  >
    <AppReserveMock slot="mockup" locale={locale} />
  </FeatureSpotlight>

  <FeatureSpotlight
    locale={locale}
    kicker={t(r.spot2_kicker, locale)}
    title={t(r.spot2_title, locale)}
    body={t(r.spot2_body, locale)}
    bullets={spot2Bullets}
    side="left"
  >
    <AppQrArriveMock slot="mockup" locale={locale} />
  </FeatureSpotlight>

  <FeatureSpotlight
    locale={locale}
    kicker={t(r.spot3_kicker, locale)}
    title={t(r.spot3_title, locale)}
    body={t(r.spot3_body, locale)}
    bullets={spot3Bullets}
    side="right"
  >
    <AppScheduleMock slot="mockup" locale={locale} />
  </FeatureSpotlight>

  <InlineCta
    h2={t(r.midcta_h2, locale)}
    sub={t(r.midcta_sub, locale)}
    ctaLabel={t(r.midcta_button, locale)}
    ctaHref={signupHref}
  />

  <Differentiators
    locale={locale}
    h2={t(r.diff_h2, locale)}
    sub={t(r.diff_sub, locale)}
    items={differentiators}
  />

  <Faq
    locale={locale}
    items={faqItems}
    heading={r.faq_h2}
    sub={r.faq_sub}
    ctaText={r.faq_cta_text}
    ctaLink={r.faq_cta_link}
  />

  <FeatureMatrix
    locale={locale}
    onlySection="scheduling"
    heading={r.matrix_h2}
    showAllToggle
  />

  <ClosingCta locale={locale} />
</Layout>
